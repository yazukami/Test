-- Script de Trade para Blox Fruits

-- Função para ativar o script apenas na cadeira de trocas
function ativarNaCadeira()
    -- Código para detectar quando o jogador está na cadeira de trocas
end

-- Painel de Controle
local painelDeControle = {
    freezing = false,
    autoForca = false
}

-- Função para congelar o jogador
function congelarJogador(jogador)
    if painelDeControle.freezing then
        -- Código para congelar o jogador
    end
end

-- Função para forçar aceitação automática
function autoForca(jogador)
    if painelDeControle.autoForca then
        -- Código para forçar o jogador a aceitar a troca
    end
end

-- Função para selecionar o jogador
function selecionarJogador(jogador)
    -- Código para selecionar o jogador
end

-- Exemplo de uso
ativarNaCadeira()
selecionarJogador("NomeDoJogador")
painelDeControle.freezing = true
congelarJogador("NomeDoJogador")
painelDeControle.autoForca = true
autoForca("NomeDoJogador")
